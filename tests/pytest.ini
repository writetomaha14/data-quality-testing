[pytest]
# Show print statements and logging output
addopts = -s -v --log-cli-level=INFO

# Disable warnings summary for cleaner output (optional)
# addopts = -s -v --log-cli-level=INFO --disable-warnings
